name: say hello

on:
  push:
    branches: [main]
    paths: ['osa11/**']

jobs:
  hello_word_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello World!"

      - name: Run date
        run: date

      - name: Run ls -l
        run: ls -l
